<section id="ticket" class="ticket-container">
    <h1>All Tickets</h1>

    {{#each tickets as |ticket|}}
      <div class="ticket-container">
        {{> ticketDetails }}
      </div>
    {{/each}}
</section>

